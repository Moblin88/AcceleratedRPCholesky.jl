name: CompatHelper
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  CompatHelper:
    runs-on: ubuntu-latest
    steps:
      - uses: julia-actions/CompatHelper.jl@v2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
